Phase5E TCP/RTU full parameter manager with tabs and oscilloscope.

Note: bundled CSV is the validated core set. Replace data/xd4000_phase5e_full_parameters.csv with the 1740-row full CSV if required.
